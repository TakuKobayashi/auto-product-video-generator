{
  "name": "demo-video-gen-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core/tsconfig.json packages/ai/tsconfig.json packages/playwright/tsconfig.json packages/voicevox/tsconfig.json packages/renderer/tsconfig.json packages/cli/tsconfig.json",
    "build:watch": "tsc -b --watch packages/*/tsconfig.json",
    "dev": "tsx packages/cli/src/index.ts",
    "clean": "find packages -name 'dist' -type d | xargs rm -rf",
    "lint": "eslint packages/*/src/**/*.ts",
    "test": "vitest run",
    "test:watch": "vitest",

    "setup": "task install",
    "setup:full": "task install PROFILE=local WITH_OLLAMA=true",
    "setup:cloud-only": "task install PROFILE=local WITH_OLLAMA=false",
    "setup:ci": "task install PROFILE=ci WITH_OLLAMA=true",

    "serve": "task serve",
    "serve:stop": "task stop",

    "doctor": "task doctor",

    "task": "task"
  },
  "devDependencies": {
    "@go-task/cli": "^3.44.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "playwright": "^1.47.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
